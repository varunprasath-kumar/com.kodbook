<!DOCTYPE html>
<html xmlns:th="http://www.thymeleaf.org">
<head>
<meta charset="UTF-8">
<title>Posts</title>
<link rel="stylesheet" href="/styles.css">

</head>
<body>

	<div class="profile-details">
		<span th:each="post : ${allPosts}">
			<p>
				<strong>Photo:</strong>
			</p> 
			<img th:if="${post.photoBase64}"
			th:src="'data:image/jpeg;base64,' + ${post.photoBase64}"
			alt="Post Photo" />

			<p>
				<strong>Caption:</strong> <span th:text="${post.caption}"></span>
			</p>
			
			<form action="/likePost" method ="get">
			<input type="hidden" name="likes" th:value="${post.id}"/>
			<input type="submit" value="Like Post" />
			</form>
			
		</span>
	</div>
</body>
</html>